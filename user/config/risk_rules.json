{
  "analysis": {
    "lineage_max_depth": 4
  },
  "thresholds": {
    "log": 5,
    "alert": 10
  },
  "rules": [
    {
      "name": "exec_from_suspicious_path",
      "description": "Process exec path is from tmp, memfd, deleted binary, or shm.",
      "feature": "execution.suspicious_path",
      "weight": 5,
      "enabled": true,
      "match": "bool_true"
    },
    {
      "name": "deep_execution_chain",
      "description": "Process lineage depth is considered deep.",
      "feature": "execution.deep_chain",
      "weight": 3,
      "enabled": true,
      "match": "bool_true"
    },
    {
      "name": "has_dangerous_capability",
      "description": "Process owns at least one dangerous capability.",
      "feature": "capability.dangerous",
      "weight": 3,
      "enabled": true,
      "match": "bool_true"
    },
    {
      "name": "seccomp_disabled",
      "description": "Process is running without seccomp.",
      "feature": "capability.seccomp_disabled",
      "weight": 2,
      "enabled": true,
      "match": "bool_true"
    },
    {
      "name": "connect_then_exec_pattern",
      "description": "Network connection followed by execve in the same process history.",
      "feature": "history.connect_then_exec",
      "weight": 6,
      "enabled": true,
      "match": "bool_true"
    },
    {
      "name": "sensitive_file_then_network",
      "description": "Sensitive file access followed by outbound network behavior.",
      "feature": "history.sensitive_then_network",
      "weight": 6,
      "enabled": true,
      "match": "bool_true"
    },
    {
      "name": "high_connect_volume",
      "description": "Process made multiple connections in a short session window.",
      "feature": "history.connect_count",
      "weight": 2,
      "enabled": true,
      "match": "number_gte",
      "min_value": 3
    }
  ]
}
